/** @type {import('next').NextConfig} */
const nextConfig = {
  // Default page extensions for Next.js
  pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
  // Optionally, add any other Next.js config below
};

module.exports = nextConfig;